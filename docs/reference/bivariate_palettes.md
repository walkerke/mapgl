# Bivariate color palettes

Bivariate color palettes

## Usage

``` r
bivariate_palettes(palette = NULL)
```

## Arguments

- palette:

  Optional palette name. If NULL, returns all palettes.

## Value

A named list of 3 by 3 color matrices, or one matrix if `palette` is
provided.
