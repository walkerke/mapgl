% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{add_terradraw_control}
\alias{add_terradraw_control}
\title{Add a Terra Draw control to a map}
\usage{
add_terradraw_control(
  map,
  position = "top-left",
  modes = NULL,
  options = NULL,
  source = NULL,
  attributes = NULL,
  point_color = "#3bb2d0",
  line_color = "#3bb2d0",
  fill_color = "#3bb2d0",
  fill_opacity = 0.1,
  active_color = "#fbb03b",
  vertex_radius = 5,
  line_width = 2,
  download_button = FALSE,
  download_filename = "drawn-features",
  show_measurements = FALSE,
  measurement_units = "both",
  orientation = "vertical"
)
}
\arguments{
\item{map}{A map object created by the \code{mapboxgl} or \code{maplibre} functions.}

\item{position}{A string specifying the position of the draw control.
One of "top-right", "top-left", "bottom-right", or "bottom-left".}

\item{modes}{A character vector of drawing modes to expose as toolbar
buttons: any of \code{"point"}, \code{"linestring"}, \code{"polygon"}, \code{"rectangle"},
\code{"circle"}, \code{"freehand"}, \code{"freehand-linestring"}, \code{"angled-rectangle"},
\code{"sector"}, \code{"sensor"}, \code{"curve"}, \code{"curve-linestring"}, and
\code{"select"}. When \code{NULL} (the default), the control shows \code{"point"},
\code{"linestring"}, \code{"polygon"}, and \code{"select"}.}

\item{options}{An object created by \code{\link[=terradraw_options]{terradraw_options()}} with advanced
Terra Draw settings (snapping, select-mode editing flags, per-mode
overrides).}

\item{source}{A character string specifying a source ID to add to the draw control.
Default is NULL.}

\item{attributes}{Optional named list defining editable feature attributes.
Use \code{\link[=draw_attribute]{draw_attribute()}} to define fields.}

\item{point_color}{Color for point features. Default is "#3bb2d0" (light blue).}

\item{line_color}{Color for line features. Default is "#3bb2d0" (light blue).}

\item{fill_color}{Fill color for polygon features. Default is "#3bb2d0" (light blue).}

\item{fill_opacity}{Fill opacity for polygon features. Default is 0.1.}

\item{active_color}{Color for active (selected) features. Default is "#fbb03b" (orange).}

\item{vertex_radius}{Radius of vertex points in pixels. Default is 5.}

\item{line_width}{Width of lines in pixels. Default is 2.}

\item{download_button}{Logical, whether to add a download button to export drawn features as GeoJSON. Default is FALSE.}

\item{download_filename}{Base filename for downloaded GeoJSON (without extension). Default is "drawn-features".}

\item{show_measurements}{Logical, whether to show live measurements while drawing. Default is FALSE.}

\item{measurement_units}{Units for measurements. Either "metric", "imperial", or "both". Default is "both".}

\item{orientation}{A string specifying the orientation of the draw control.
Either "vertical" (default) or "horizontal".}
}
\value{
The modified map object with the draw control added.
}
\description{
A convenience wrapper around \code{\link[=add_draw_control]{add_draw_control()}} with
\code{provider = "terra-draw"}, exposing only the arguments that apply to the
Terra Draw engine. The two forms are equivalent; see \code{\link[=add_draw_control]{add_draw_control()}}
for full details of the terra-draw provider's behavior, and
\code{\link[=terradraw_options]{terradraw_options()}} for advanced configuration.
}
\examples{
\dontrun{
library(mapgl)

maplibre() |>
    add_terradraw_control()

maplibre() |>
    add_terradraw_control(
        modes = c("polygon", "rectangle", "circle", "freehand", "select"),
        options = terradraw_options(snap_to_coordinates = TRUE),
        download_button = TRUE
    )
}
}
