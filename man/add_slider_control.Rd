% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slider.R
\name{add_slider_control}
\alias{add_slider_control}
\title{Add a slider control to a map}
\usage{
add_slider_control(
  map,
  layers,
  property = NULL,
  time_unit = NULL,
  values = NULL,
  labels = NULL,
  min = NULL,
  max = NULL,
  step = 1,
  mode = c("sequential", "cumulative", "window"),
  window = NULL,
  presentation = c("compact", "timeline"),
  window_behavior = c("auto", "resizable", "fixed"),
  histogram = FALSE,
  histogram_data = NULL,
  counts = NULL,
  initial_value = NULL,
  paint_property = NULL,
  paint_expressions = NULL,
  paint_properties = NULL,
  play_button = FALSE,
  animation_duration = 1000,
  loop = TRUE,
  title = NULL,
  show_value = TRUE,
  slider_style = NULL,
  width = 280,
  position = "top-left",
  draggable = FALSE,
  background_color = "#ffffffcc",
  text_color = "#404040",
  accent_color = "#4a90e2"
)
}
\arguments{
\item{map}{A map object created by \code{\link[=mapboxgl]{mapboxgl()}} or \code{\link[=maplibre]{maplibre()}}, or a
proxy object from \code{\link[=mapboxgl_proxy]{mapboxgl_proxy()}} / \code{\link[=maplibre_proxy]{maplibre_proxy()}}.}

\item{layers}{Character vector of layer IDs the slider should affect.
Layers need not exist yet — if a matching layer is added later via
proxy, the slider's current filter and/or paint expression is
applied on its first paint.}

\item{property}{Name of the numeric feature property to filter on.
Optional: if \code{NULL}, no filter behavior. At least one of \code{property}
or \code{paint_property} must be supplied.}

\item{time_unit}{Optional. Only consequential when a target layer is a
flowmap (\code{add_flowmap()}): ordinary layers filter on the numeric
\code{property} directly, but flowmap layers need an absolute timestamp
range, so in \code{"window"} mode the numeric value is converted to a
flowmap time range using this unit. One of \code{"seconds"}, \code{"date"}, or
\code{"year"} (the absolute units produced by \code{\link[=as_time_property]{as_time_property()}}).
\code{"month"}/\code{"day"} are calendar indices, not instants, and cannot
drive a flowmap window.}

\item{values}{Numeric vector of steps. Required unless \code{min} and
\code{max} are supplied.}

\item{labels}{Optional character vector of display labels (one per
value). When omitted, labels default to \code{as.character(values)}, except
when \code{time_unit} is \code{"seconds"} or \code{"date"}: then the numeric values are
formatted back into readable timestamps (\code{"\%Y-\%m-\%d \%H:\%M"} in UTC for
seconds, \code{"\%Y-\%m-\%d"} for dates) so a time-driven slider does not display
raw epoch numbers. Pass an explicit vector to localize or change the
format.}

\item{min, max, step}{Numeric range specification used when \code{values} is
not supplied. \code{values} is generated via \code{seq(min, max, by = step)}.}

\item{mode}{One of \code{"sequential"} (default) — each step shows only
features matching that exact value; \code{"cumulative"} — shows
everything up through the current value; or \code{"window"} — shows a
moving \verb{[start, end]} range (the end is the current value). Window
mode emits a range filter for ordinary layers and a
\code{selectedTimeRange} for flowmap layers, making it the mode used to
drive temporal flowmaps. Only applies when \code{property} is set.}

\item{window}{Only used when \code{mode = "window"}. \code{NULL} (default) makes
a cumulative range \verb{[min(values), T]}; a positive number makes a
sliding window \verb{[T - window, T]}, where \code{window} is expressed in the
same units as \code{values} (e.g. for \code{\link[=as_time_property]{as_time_property()}} \code{unit = "date"}, \code{window = 7} is a 7-day window).}

\item{presentation}{Visual style of the control. \code{"compact"} (default) is
a small slider (optionally with a density strip behind it). \code{"timeline"}
renders a prominent, brushable histogram as the control itself -- drag the
selected window across the bars, or drag its edges to resize it (as in
FlowMapBlue's time control). \code{"timeline"} requires histogram data
(\code{counts} or \code{histogram_data}) and implies \code{histogram = TRUE}.}

\item{window_behavior}{Only used when \code{mode = "window"}. \code{"auto"} (default)
uses a fixed-duration window when \code{window} is supplied and a resizable
range otherwise. \code{"fixed"} pins the window width to \code{window} and moves the
whole \verb{[end - window, end]} band. \code{"resizable"} lets the user drag either
edge to resize the selected range.}

\item{histogram}{Logical; if \code{TRUE}, draw a density strip behind the
slider showing how many features fall at each value (a data-density
backdrop, most useful with \code{mode = "window"} where the selected band
highlights the covered bars). Loads d3 on demand. Supply the bar
heights via \code{counts} or \code{histogram_data}. Default \code{FALSE}.}

\item{histogram_data}{Numeric vector of the raw property values across
your features; binned in R to the nearest \code{values} step to produce
the histogram bar heights. Use this when you have the underlying data
to hand. Ignored unless \code{histogram = TRUE}.}

\item{counts}{Numeric vector of pre-computed bar heights, one per
\code{values}. An alternative to \code{histogram_data} when you already have
per-step counts. Ignored unless \code{histogram = TRUE}.}

\item{initial_value}{Value to start on. In \code{"window"} mode this is the
window end value and defaults to the last value; other modes default
to the first value.}

\item{paint_property}{Optional Mapbox paint property the slider
should animate, supplied in snake_case to match the rest of the
package (e.g. \code{"fill_color"}, \code{"fill_extrusion_height"},
\code{"circle_radius"}). Kebab-case is also accepted for convenience
when copying from Mapbox docs. Shortcut for the single-property
case; when set, \code{paint_expressions} must also be supplied. For
animating more than one paint property together (e.g. height \emph{and}
color), use \code{paint_properties} instead.}

\item{paint_expressions}{List of Mapbox expressions, one per value in
\code{values}. On each step, the expression at the matching index is
applied to each target layer via \code{map.setPaintProperty()}. Build
expressions with \code{\link[=interpolate]{interpolate()}}, \code{\link[=match_expr]{match_expr()}}, or raw lists.}

\item{paint_properties}{Optional named list for animating multiple
paint properties simultaneously. Shape:
\verb{list(fill_color = list(<expr_0>, <expr_1>, ...), fill_extrusion_height = list(...))}
— names are snake_case (kebab-case also accepted), each value is a
list of expressions the same length as \code{values}. Cannot be
combined with \code{paint_property}/\code{paint_expressions}. Each property's
pre-slider value is captured on mount and restored on removal,
independently.}

\item{play_button}{Logical; include a play/pause button next to the
slider. Default \code{FALSE}.}

\item{animation_duration}{Milliseconds to wait between steps while
playing. Default \code{1000}.}

\item{loop}{Logical; when playback reaches the last value, return to
the first. Default \code{TRUE}.}

\item{title}{Optional title shown above the slider.}

\item{show_value}{Logical; show the current label beside the slider.
Default \code{TRUE}.}

\item{slider_style}{Optional appearance for the slider: a preset string
(\code{"light"}, \code{"dark"}, or \code{"auto"}) or a \code{\link[=slider_style]{slider_style()}} object. When
omitted, the legacy \code{width}, \code{background_color}, \code{text_color}, and
\code{accent_color} arguments are used.}

\item{width}{Slider container width in pixels. Default \code{280}.}

\item{position}{One of \code{"top-left"}, \code{"top-right"}, \code{"bottom-left"},
\code{"bottom-right"}. Default \code{"top-left"}.}

\item{draggable}{Logical, whether the slider panel can be dragged to a new
position on the map by the user. Default \code{FALSE}, keeping it docked at
\code{position}.}

\item{background_color, text_color, accent_color}{Styling overrides.
Defaults match the package's other controls. For full control of the
slider appearance, use \code{slider_style}.}
}
\value{
The modified map or proxy object, invisibly.
}
\description{
Adds an interactive slider to a Mapbox GL or MapLibre GL map that
steps through a sequence of values and filters one or more layers by
a numeric feature property. Works source-agnostically: GeoJSON,
vector tile, and PMTiles layers all compose through the same filter
pipeline. Optional play button animates through the steps;
\code{"cumulative"} mode accumulates features through the range rather
than swapping them.
}
\details{
Values commonly represent time (years, months, epoch seconds) but
need not — any monotonic numeric sequence works (magnitude
thresholds, percentile coverage, depth bins, etc.).

The slider composes with other filter sources rather than replacing
them: a layer's initial \verb{filter =} argument, a subsequent
\code{\link[=set_filter]{set_filter()}} call, and an interactive \code{\link[=add_legend]{add_legend()}} can all be
active at the same time as the slider, and the layer's resolved
filter is the \verb{["all", ...]} intersection.
}
\section{Date and time values}{

The slider requires numeric values. Because \code{sf} objects are
serialized to GeoJSON via \code{geojsonsf::sf_geojson()}, \code{Date} and
\code{POSIXct} columns become JSON strings, not numbers — so you must
pre-coerce your time column to numeric before adding the layer. Use
\code{\link[=as_time_property]{as_time_property()}} for the common cases (year, month index, epoch
seconds, days-since-epoch).
}

\section{Positioning and collisions}{

The slider is implemented as a native control and stacks beside
other native controls (navigation, scale, geolocate, fullscreen) via
Mapbox/MapLibre's built-in positioning. Overlays such as
\code{\link[=add_legend]{add_legend()}} and \code{\link[=add_layers_control]{add_layers_control()}} are absolutely-positioned
and do not participate in that flow — placing a slider in the same
corner as a legend will overlap it. Choose a different corner or
adjust the overlay's margins.
}

\examples{
\dontrun{
library(mapgl)
library(sf)

quakes <- sf::read_sf(
  "https://docs.mapbox.com/mapbox-gl-js/assets/significant-earthquakes-2015.geojson"
)
quakes$month <- as.POSIXlt(quakes$time / 1000, origin = "1970-01-01")$mon

mapboxgl(center = c(-45, 0), zoom = 0.25) |>
  add_circle_layer(
    id = "quakes",
    source = quakes,
    circle_color = "#FCA107",
    circle_radius = 20,
    circle_opacity = 0.75
  ) |>
  add_slider_control(
    layers = "quakes",
    property = "month",
    values = 0:11,
    labels = month.name,
    title = "Significant earthquakes in 2015",
    play_button = TRUE
  )

# --- Paint animation: step through a choropleth's color over years
# Given an sf object with columns pop2020, pop2021, ..., pop2024:
# mapboxgl() |>
#   add_fill_layer(id = "tracts", source = tracts, fill_opacity = 0.85) |>
#   add_slider_control(
#     layers = "tracts",
#     values = 2020:2024,
#     paint_property = "fill_color",
#     paint_expressions = lapply(2020:2024, function(y) {
#       interpolate(
#         column = paste0("pop", y),
#         values = c(0, 5e5, 2e6),
#         stops  = c("#f7f4f9", "#67a9cf", "#02818a")
#       )
#     }),
#     play_button = TRUE
#   )
}

}
\seealso{
\code{\link[=update_slider_control]{update_slider_control()}}, \code{\link[=as_time_property]{as_time_property()}}
}
