% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screenshot.R
\name{basemap_style}
\alias{basemap_style}
\title{Create a blank basemap style}
\usage{
basemap_style(color = "white", pattern = NULL)
}
\arguments{
\item{color}{Character string. The background color. Default \code{"white"}.
Accepts any CSS color value (e.g., \code{"#f0f0f0"}, \code{"lightgrey"},
\code{"rgba(0,0,0,0)"}). Also used as a fallback behind transparent areas of
a \code{pattern}.}

\item{pattern}{Character string or \code{NULL}. The ID of an image to use as a
repeating background pattern. The image must be loaded with \code{\link[=add_image]{add_image()}}
before it can be referenced. Default \code{NULL} (solid color only).}
}
\value{
A list representing a minimal map style, suitable for passing to
the \code{style} parameter of \code{\link[=maplibre]{maplibre()}} or \code{\link[=mapboxgl]{mapboxgl()}}.
}
\description{
Creates a minimal map style with only a solid background color (or pattern)
and no basemap tiles. Useful when you want to display data layers without
any underlying map features.
}
\examples{
\dontrun{
library(mapgl)

# Solid color background
maplibre(style = basemap_style("lightgrey")) |>
  add_fill_layer(
    id = "data",
    source = my_sf_data,
    fill_color = "steelblue"
  )

# Background pattern (image must be loaded with add_image())
maplibre(style = basemap_style(pattern = "parchment")) |>
  add_image("parchment", "parchment.jpg") |>
  add_line_layer(
    id = "borders",
    source = my_sf_data,
    line_color = "#2c1810"
  )
}
}
