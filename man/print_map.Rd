% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screenshot.R
\name{print_map}
\alias{print_map}
\title{Render a map as a static image}
\usage{
print_map(
  map,
  width = 900,
  height = 500,
  include_legend = TRUE,
  hide_controls = TRUE,
  include_scale_bar = TRUE,
  basemap_color = NULL,
  image_scale = 1,
  background = "white",
  delay = NULL
)
}
\arguments{
\item{map}{A map object created by \code{\link[=mapboxgl]{mapboxgl()}} or \code{\link[=maplibre]{maplibre()}}.}

\item{width}{Integer. The width of the map viewport in pixels.}

\item{height}{Integer. The height of the map viewport in pixels.}

\item{include_legend}{Logical. Include the legend in the output? Default
\code{TRUE}.}

\item{hide_controls}{Logical. Hide navigation and other interactive controls?
Default \code{TRUE}.}

\item{include_scale_bar}{Logical. Include the scale bar? Default \code{TRUE}.}

\item{basemap_color}{Character string or \code{NULL}. If specified, basemap tiles
are removed and replaced with this background color (e.g., \code{"white"},
\code{"lightgrey"}, \code{"#f0f0f0"}). Use \code{"transparent"} for no background.
Default \code{NULL} (keep basemap).}

\item{image_scale}{Numeric. Scale factor for the output image. Use \code{2} for
retina/HiDPI output. Default \code{1}.}

\item{background}{Character string or \code{NULL}. Background color for the output
image. Default \code{"white"}. Set to \code{NULL} for a transparent background.
Ignored when \code{basemap_color} is set (basemap_color controls the background
in that case).}

\item{delay}{Numeric or \code{NULL}. Additional delay in seconds to wait after the
map reports idle, before capturing. Useful for maps with complex rendering.
Default \code{NULL} (no extra delay).}
}
\value{
In a knitr context, the result of \code{\link[knitr:include_graphics]{knitr::include_graphics()}}.
In an interactive session, the image is displayed and the temporary file
path is returned invisibly.
}
\description{
Renders a mapgl map as a static PNG image for display. When called inside a
knitr/Quarto document, the map is included as a static figure via
\code{\link[knitr:include_graphics]{knitr::include_graphics()}}. In an interactive session, the image is
displayed in the R graphics device.
}
\examples{
\dontrun{
library(mapgl)

map <- maplibre(
  center = c(-96, 37.8),
  zoom = 3
)

# In a Quarto document chunk
print_map(map)

# With custom dimensions
print_map(map, width = 1200, height = 800, image_scale = 2)
}
}
