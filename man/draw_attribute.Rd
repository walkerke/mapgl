% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{draw_attribute}
\alias{draw_attribute}
\title{Define an editable draw attribute}
\usage{
draw_attribute(
  type = NULL,
  label = NULL,
  choices = NULL,
  default = NULL,
  required = FALSE,
  placeholder = NULL,
  min = NULL,
  max = NULL,
  step = NULL
)
}
\arguments{
\item{type}{Input type for the editor. Supported values are \code{"text"},
\code{"textarea"}, \code{"select"}, \code{"number"}, and \code{"checkbox"}. \code{"numeric"} is an
alias for \code{"number"}; \code{"logical"}, \code{"bool"}, and \code{"boolean"} are aliases
for \code{"checkbox"}. If \code{NULL}, the type is inferred from \code{choices} or
\code{default}.}

\item{label}{Optional label shown in the editor. Defaults to the field name.}

\item{choices}{Values for \code{"select"} fields. Names, if present, are used as
labels and values are written to feature properties.}

\item{default}{Optional default value. Defaults are applied to newly drawn
features only; existing feature properties are preserved.}

\item{required}{Logical; whether the browser should require a value before
saving.}

\item{placeholder}{Optional placeholder for text, textarea, and number
inputs.}

\item{min, max, step}{Optional numeric input constraints for \code{"number"} fields.}
}
\value{
A list suitable for one entry in \code{add_draw_control(attributes = )}.
}
\description{
This helper creates one field definition for the \code{attributes} argument in
\code{\link[=add_draw_control]{add_draw_control()}}. The field name comes from the name used in the
\code{attributes} list.
}
\examples{
draw_attribute("select", choices = c("candidate", "active", "rejected"))
draw_attribute("textarea", label = "Notes")
draw_attribute("numeric", min = 0, max = 1, step = 0.1, default = 1)

\dontrun{
mapboxgl() |>
  add_draw_control(
    attributes = list(
      status = draw_attribute(
        "select",
        choices = c(Candidate = "candidate", Active = "active")
      ),
      notes = draw_attribute("textarea"),
      value = draw_attribute("numeric")
    )
  )
}
}
