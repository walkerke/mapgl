% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{terradraw_options}
\alias{terradraw_options}
\title{Configure the Terra Draw provider for the draw control}
\usage{
terradraw_options(
  drag_features = TRUE,
  rotate_features = FALSE,
  scale_features = FALSE,
  drag_vertices = TRUE,
  delete_vertices = TRUE,
  midpoints = TRUE,
  resize = NULL,
  snap_to_coordinates = FALSE,
  snap_to_lines = FALSE,
  editable_while_drawing = FALSE,
  show_coordinate_points = FALSE,
  drag_interaction = NULL,
  pointer_distance = NULL,
  keep_mode_active = FALSE,
  modes = NULL
)
}
\arguments{
\item{drag_features}{Logical, whether selected features can be moved by
dragging. Default is \code{TRUE}.}

\item{rotate_features}{Logical, whether selected features can be rotated.
Default is \code{FALSE}.}

\item{scale_features}{Logical, whether selected features can be scaled.
Default is \code{FALSE}.}

\item{drag_vertices}{Logical, whether individual vertices of a selected
feature can be dragged. Default is \code{TRUE}.}

\item{delete_vertices}{Logical, whether individual vertices of a selected
feature can be deleted. Default is \code{TRUE}.}

\item{midpoints}{Logical, whether midpoints are shown between vertices so
new vertices can be inserted. Default is \code{TRUE}. Ignored (treated as
\code{FALSE}) when \code{resize} is set, as Terra Draw does not support both.}

\item{resize}{How selected features may be resized by dragging their
selection points: one of \code{"center"}, \code{"opposite"}, \code{"center-fixed"}, or
\code{"opposite-fixed"}. Default \code{NULL} disables resizing.}

\item{snap_to_coordinates}{Logical, whether drawing and editing snap to
existing feature coordinates. Default is \code{FALSE}.}

\item{snap_to_lines}{Logical, whether drawing and editing snap to existing
feature lines. Default is \code{FALSE}.}

\item{editable_while_drawing}{Logical, whether features can be edited
(vertices dragged) while still being drawn, for the point, linestring,
and polygon modes. Default is \code{FALSE}.}

\item{show_coordinate_points}{Logical, whether coordinate points are
displayed on linestring and polygon features while drawing. Default is
\code{FALSE}.}

\item{drag_interaction}{How rectangle and circle drawing works: one of
\code{"click-move"} (click, move, click), \code{"click-drag"}, or
\code{"click-move-or-drag"}. Default \code{NULL} uses the Terra Draw default.}

\item{pointer_distance}{Pointer tolerance in pixels for drawing
interactions. Default \code{NULL} uses the Terra Draw default.}

\item{keep_mode_active}{Logical. By default (\code{FALSE}) the toolbar returns
to the select tool after a shape is finished, matching the
mapbox-gl-draw workflow; set to \code{TRUE} to stay in the active drawing
mode instead.}

\item{modes}{Advanced escape hatch: a named list keyed by mode name whose
entries are raw Terra Draw mode constructor options (in Terra Draw's own
camelCase naming, e.g. \code{list(polygon = list(pointerDistance = 30))}).
These are merged over what mapgl generates, with \code{styles} entries merged
key by key. \code{modeName} overrides are not allowed.}
}
\value{
A list of class \code{"mapgl_terradraw_options"} for use as the
\code{options} argument of \code{\link[=add_draw_control]{add_draw_control()}}.
}
\description{
This helper builds the \code{options} argument for
\code{\link[=add_draw_control]{add_draw_control()}} with \code{provider = "terra-draw"}. All arguments are
optional; the defaults give a fully editable select mode without snapping.
}
\examples{
\dontrun{
library(mapgl)

maplibre() |>
    add_draw_control(
        provider = "terra-draw",
        options = terradraw_options(
            snap_to_coordinates = TRUE,
            rotate_features = TRUE
        )
    )
}
}
