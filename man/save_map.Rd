% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screenshot.R
\name{save_map}
\alias{save_map}
\title{Save a map as a static PNG image}
\usage{
save_map(
  map,
  filename = "map.png",
  width = 900,
  height = 500,
  include_legend = TRUE,
  hide_controls = TRUE,
  include_scale_bar = TRUE,
  basemap_color = NULL,
  image_scale = 1,
  background = "white",
  delay = NULL
)
}
\arguments{
\item{map}{A map object created by \code{\link[=mapboxgl]{mapboxgl()}} or \code{\link[=maplibre]{maplibre()}}.}

\item{filename}{Character string. The output file path. Defaults to
\code{"map.png"}. If the filename does not end in \code{.png}, the extension is
appended automatically.}

\item{width}{Integer. The width of the map viewport in pixels.}

\item{height}{Integer. The height of the map viewport in pixels.}

\item{include_legend}{Logical. Include the legend in the output? Default
\code{TRUE}.}

\item{hide_controls}{Logical. Hide navigation and other interactive controls?
Default \code{TRUE}.}

\item{include_scale_bar}{Logical. Include the scale bar? Default \code{TRUE}.}

\item{basemap_color}{Character string or \code{NULL}. If specified, basemap tiles
are removed and replaced with this background color (e.g., \code{"white"},
\code{"lightgrey"}, \code{"#f0f0f0"}). Use \code{"transparent"} for no background.
Default \code{NULL} (keep basemap).}

\item{image_scale}{Numeric. Scale factor for the output image. Use \code{2} for
retina/HiDPI output. Default \code{1}.}

\item{background}{Character string or \code{NULL}. Background color for the output
image. Default \code{"white"}. Set to \code{NULL} for a transparent background.
Ignored when \code{basemap_color} is set (basemap_color controls the background
in that case).}

\item{delay}{Numeric or \code{NULL}. Additional delay in seconds to wait after the
map reports idle, before capturing. Useful for maps with complex rendering.
Default \code{NULL} (no extra delay).}
}
\value{
The output file path, invisibly.
}
\description{
Renders a mapgl map widget to a static PNG file using headless Chrome
via the chromote package. Uses the same html2canvas-based screenshot
infrastructure as \code{\link[=add_screenshot_control]{add_screenshot_control()}}.
}
\details{
This function requires the \pkg{chromote} package and a Chrome or Chromium
browser installation. Install chromote with \code{install.packages("chromote")}.

The function works by:
\enumerate{
\item Saving the map widget to a temporary HTML file
\item Opening it in headless Chrome
\item Waiting for all map tiles and styles to load
\item Using html2canvas to capture the rendered map (including legends,
attribution, and optionally the scale bar)
\item Decoding the captured image and writing it to the output file
}
}
\examples{
\dontrun{
library(mapgl)

map <- maplibre(
  center = c(-96, 37.8),
  zoom = 3
)

save_map(map, "us_map.png")
save_map(map, "us_map_retina.png", image_scale = 2)

# Remove basemap, keep only data layers on white
save_map(map, "data_only.png", basemap_color = "white")
}
}
