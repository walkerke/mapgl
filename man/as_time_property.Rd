% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slider.R
\name{as_time_property}
\alias{as_time_property}
\title{Coerce a time column to a numeric property for filtering}
\usage{
as_time_property(x, unit = c("year", "month", "day", "date", "seconds"))
}
\arguments{
\item{x}{A \code{Date}, \code{POSIXct}, or numeric vector.}

\item{unit}{One of:
\itemize{
\item \code{"year"} — calendar year as integer (e.g. 2026).
\item \code{"month"} — zero-based month index (0 = January).
\item \code{"day"} — day of month.
\item \code{"date"} — days since \code{1970-01-01}.
\item \code{"seconds"} — seconds since the Unix epoch.
}}
}
\value{
An integer or numeric vector appropriate for attaching to an
\code{sf} object as a new column before calling
\code{\link[=add_slider_control]{add_slider_control()}}.
}
\description{
Helper for preparing a \code{Date} or \code{POSIXct} column for use with
\code{\link[=add_slider_control]{add_slider_control()}}. Because \code{sf} objects are serialized to
GeoJSON as JSON strings for date/time columns, the slider needs a
numeric counterpart to filter on.
}
\examples{
dates <- as.Date(c("2015-01-15", "2015-06-30", "2015-12-31"))
as_time_property(dates, "month")
as_time_property(dates, "year")
as_time_property(dates, "date")

}
